const context = "/api";
export const URLs = {
  login: context + "/login",
  logout: context + "/logout",
  getProfile: context + "/user/getProfile",
  getMessages: context + "/history",

  websocket: context + "/stomp-registry",

};